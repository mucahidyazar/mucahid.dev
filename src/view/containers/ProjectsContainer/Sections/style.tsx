import styled from 'styled-components'

const SectionWrapper = styled.div``
const SectionCards = styled.div`
  height: 600px;
  overflow-y: scroll;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
`

export {SectionWrapper, SectionCards}
